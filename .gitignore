# Node / Next
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Env (NUNCA commitear secretos)
.env
.env.local
.env.*.local
*/.env

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build
dist/
build/

# supabase cli
supabase/.temp/
supabase/.branches/

# memoria de sesiones de Claude (puede contener credenciales, nunca commitear)
memory/

# Las placas y el video se generan con scripts/video-promo.mjs contra el sitio
# real. Versionar 3 MB de PNG que se rehacen en dos minutos —y que envejecen
# cada vez que cambia una pantalla— es guardar una foto vieja del producto.
frontend/video-promo/
capturas/
