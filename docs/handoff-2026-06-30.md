# Handoff — estado del proyecto (2026-06-30)

> Resumen para continuar en otra sesión (p. ej. abriendo el workspace
> `encontralo-reservalo.code-workspace`). Leé esto primero.

## Ecosistema (una sola app)
- **Encontralo** = mapa/descubrimiento + **PWA instalable**. `encontralo.store`.
  Repo `saadypacheco/BermejoLives` (`c:\repos\proyectosClaude\Bermejo`).
  Containers: `buscadonde-frontend` (3003) · `buscadonde-backend` (8000).
- **Reservalo** = tienda e-commerce **white-label**. Montada en
  `encontralo.store/reservalo` (mismo origen → misma PWA). `reservalo.store` → 301.
  Repo `saadypacheco/reservalo` (`c:\repos\proyectosClaude\reservalo`).
  Containers: `reservalo-frontend` · `reservalo-backend`.
- **Repos SEPARADOS, workflow unificado** (ver `docs/arquitectura-repos.md`).
- Stack: Next.js 14 + FastAPI + Supabase Cloud. Traefik en el VPS (red `traefik`,
  certresolver `letsencrypt`). Deploy: `docker compose -f docker-compose.prod.yml --env-file .env up -d --build`.

## Qué se hizo en esta sesión (Encontralo)
- **Home mobile-first**: header oscuro (marca + "EN EL MAPA" + buscador + "Publicá tu negocio"), chips de texto simples, mapa full, bottom-nav (Inicio · Guardados · Ofertas · Perfil · Salir).
- **Tocar un pin** → tarjeta flotante sobre el mapa + **flecha punteada** pin→tarjeta + **ofertas del negocio** dentro de la tarjeta.
- **/buscar** rediseñado con el estilo del home.
- **Ofertas con descuento**: campos `descuento_pct` + `vence_el` (migración **0020** + vista `feed_publico`), badges "-20%" / "Válido hasta". Alta en el chatbot `/autoregistro` y **edición/baja** en panel "Mi comercio → Mis ofertas" (`PATCH/DELETE /comercio/publicaciones/{id}`, 71 tests backend OK).
- **"Ofertas"** del bottom-nav filtra el mapa del home (`?of=1`), sin página nueva.
- **Performance internet malo**: tiles `updateWhenIdle`, imágenes `lazy`.
- **PWA**: `manifest.ts` (scope `/`), `public/sw.js` (cache de tiles + app-shell), íconos PNG, registro en layout.
- **Integración Reservalo**: la otra sesión montó la tienda en `encontralo.store/reservalo` (basePath `/reservalo`, Traefik `priority=1000`, `skipTrailingSlashRedirect`).

## Estado de deploy
- Todo el código está **pusheado a `main`** (Encontralo). Último commit relevante: PWA.
- ⚠️ **Falta correr el deploy en el VPS** para que prod tome lo último (flecha + perf + PWA). Ver `scripts/deploy-all.sh`.

## Pendientes (prioridad arriba)
1. **Deployar** lo último a prod (VPS) y probar la **PWA instalada** en el celu.
2. **Confirmar la flecha** pin→tarjeta en mobile (si no, plan B: tarjeta con "colita" tipo globo).
3. **Conectar productos**: cambiar los links de Encontralo de `https://reservalo.store` → **`/reservalo/...`** (mismo origen) y traer productos por API (`TiendaClient`/`producto_ref`) para mostrarlos dentro de Encontralo. (Hay `RESERVALO_URL` hardcodeado en `search-hero.tsx` y `buscar/page.tsx`.)
4. **Cargar ofertas reales** en la cloud (hoy 0 → rail/badge vacíos en prod).
5. **Subir foto** en alta/edición de ofertas (hoy es link de imagen). **"Guardados"** (placeholder, falta la función).
6. **QR reales** de pago + **`GEMINI_API_KEY`** (clasificador real).
7. **Seguridad**: rotar `service_role` + password DB + confirmar `WEBHOOK_SECRET`.
8. Fase 2: **Auth unificada + OTP**. Cutover de `buscadonde.com`.

## Datos sensibles / recordatorios
- Migraciones a la cloud: **session pooler** (`aws-1-us-west-2.pooler.supabase.com:5432`), no directo.
- `SUPABASE_SERVICE_ROLE_KEY` solo en backend, nunca al frontend.
- `MODO_CAPTURA=1` hoy (sitio en captura). Para abrir público: `0` + rebuild frontend.
